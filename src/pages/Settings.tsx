import { Box, Text } from '@chakra-ui/react';

const Settings = () => {
  return (
    <Box bg="gray.800" color="gray.100" p={8} minH="100vh">
      <Text fontSize="2xl" mb={4}>Settings</Text>
      {/* Add settings interface components here */}
    </Box>
  );
};

export default Settings;
